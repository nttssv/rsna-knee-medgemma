# Project continuity

- For any temporary RunPod API key or shutdown-control task, first read [docs/RUNPOD_TEMPORARY_KEY_SOP.md](docs/RUNPOD_TEMPORARY_KEY_SOP.md). It contains the exact UI permissions, one-shot local secret intake, mandatory application User-Agent, exact-pod checks, and disable-after-stop procedure for Luna/Sol/Astra. Reuse an existing permitted key before creating another; never infer invalid credentials from a default-urllib 403 probe. Do not open a new window or print credentials.

- This GitHub repository is public by user request on 2026-09-14. Keep private data, credentials and model artifacts excluded from Git.
- Read README.md, docs/EXPERIMENT.md, and docs/MIGRATION.md before starting work.
- Current focus: Qwen report extraction, by user direction on 2026-09-15. MedGemma development and YOLO work are paused.
- Read analysis/README.md and analysis/report_labeling/RESULTS.md before further label or model work. MRI training and bulk extraction on the 4,349 reports remain paused.
- The separate analysis/report_labeling_llm_v1/ benchmark completed five development reports twice/model at executed commit f334958. Both all-valid repeatability gates failed. Read README.md, RESULTS.md, REVIEW.md and RESOURCES.md. RunPod was stopped; no full 40-study development or 18-study validation LLM inference or bulk labeling occurred. Preserve the execution fingerprints and exact split. Any further GPU stage needs a concrete reviewed version and current user resource authorization.
- The analysis/report_labeling_llm_v2/ RTX 6000 Ada smoke ran at f09a4ddc3b3a795f97766686adc05a74b75a1fcc and FAILED after three MedGemma reports: 24 parse errors, 12 truncated cells, zero accepted labels. Remaining repeats and Qwen NOT RUN. Read RTX_SMOKE_RESULTS.md and RESULTS.md. All 17 original artifacts were copied and hash-verified; GPU is stopped. No automatic restart or retry. Any revised generation recipe must be prospective and separately reviewed/authorized; the local response_diagnostic_v1 investigation is complete. Read its RESULTS.md: strict prefix removal yields 20/24 completed-case technical passes but retains clear polarity/scope issues; third response used 1,559 thought and 487 answer tokens before truncation. No working thinking-disable flag was found in the pinned template. Any future design must separately address framing, completion and semantics, without retroactively rescoring v2. Preserve v2 executed scripts/configs/prompts/tests/smoke_review_tools/PROTOCOL.md byte-identically, as well as both v1 directories and all historical private outputs. The separate diagnostics/ viewer handles this partial failure without weakening the existing completed-session gate. The 12 private development adjudication items remain unresolved; ChatGPT advice is not clinical adjudication.
- The analysis/report_labeling_llm_v3/ LOCAL candidate is prepared/tokenized with execution disabled and no GPU adapter. Read README.md, PROTOCOL.md, RESULTS.md and REVIEW.md. It compares exact v2 prompt against an evidence-first prompt with shared strict thought-envelope handling and 4096 output tokens, MedGemma only, five known development reports, ABBA order and 20 planned generations. No measured v3 outputs or semantic improvement exists. Preserve its prepared fingerprints and all 124 historical tracked files. Do not mistake 24 synthetic expectations or software tests for LLM results. The next scoped implementation would be a separately reviewed bounded adapter; no paid run or scaling is authorized by this candidate.
- Keep the report-labeling v1 source/config/tests byte-identical to analysis/report_labeling/frozen_code_manifest.json. Its 18-study holdout has been inspected; make future fixes in a separately versioned experiment with a new validation plan.
- The separate analysis/report_labeling_llm_v3_runtime_v1/ implements the local bounded runner and blinded review workflow. Read README.md, PROTOCOL.md and RESULTS.md. Its GPU guard is hard-disabled, including --execute; CPU demo outputs are fabricated and are not MedGemma results. Preserve the v3 candidate and all 145 prior tracked analysis files. Serve only a review bundle's blinded/ directory; operator mappings and organizer references are separate. Blank/unreviewed cells never count as semantic passes, and no tool certifies reviewer qualifications or authorizes scaling. A later paid stage requires a separately reviewed execution revision and current resource authorization.
- Per-study report-label outputs and evidence belong in ignored state/runs/report-labeling-20260913-v1, never in analysis/ or Git. Only aggregate findings are committed.
- The separate analysis/report_labeling_llm_v3_execution_v1/ is an authorization-gated execution proposal. Preserve all 156 prior analysis files, including the hard-disabled runtime. Read its PROTOCOL.md and resource proposal. Real generation requires current explicit private plan/proposal-bound user approval, verified actual rates, matching RTX 6000 Ada hardware and a live pod-local self-stop watchdog; no approval is created by preparation. Proposed limits are $2 and 90 minutes including setup/transfer, with the existing 60-minute inference cap and five-minute copy/stop reserve. Confirm provider stop externally; watchdog success is not billing confirmation. The quote expires after 24 hours. No broader inference/training/provisioning is authorized by this proposal.
- The completed pilot is preserved under state/runs/20260913T135620Z locally; it is not in GitHub. Inspect it before assuming training must start over.
- The original adapter supports inference and warm-start training, not exact optimizer resume. New-format checkpoints include full training state.
- Keep credentials, competition metadata, per-study IDs, MRI files, and model weights out of Git. Use state/ and a private backup.
- Use RSNA_STATE_DIR for persistent paths. Do not embed provider IPs, pod IDs, SSH keys, usernames, or mount paths in training code.
- CPU audits and checks are local and do not require starting a GPU. Do not provision paid compute implicitly.
- Before committing, run relevant tests and python scripts/check_staged.py after staging explicit source paths.
- Do not overwrite an existing run. Preserve split and input fingerprints when comparing experiments.
- The scientific claims are limited to a small feasibility pilot. Scores are uncalibrated Yes/No rankings; case labels describe full studies, not the sampled slices alone.
- For print requests, default to duplex and black-and-white unless the user specifies otherwise.

- The approved v3 replacement setup on 2026-09-15 stopped before model loading: the provider pod-scoped key failed required read access (legacy Unauthorized, modern CLI and GraphQL HTTP 403). Read analysis/report_labeling_llm_v3_execution_v1/SETUP_ATTEMPT.md. No model download or generation occurred; the replacement is stopped with zero idle disk charge. Validate a supported read/self-stop authorization path locally before any further paid start. Read denial does not establish stop denial. Preserve the existing plan and do not bypass its watchdog gate.

- Recovery: user-approved temporary GraphQL control key passed local read/stop against the stopped Pod after the HTTP helper supplied an explicit application User-Agent. Read CONTROL_PREFLIGHT.md in the execution package. A resume attempt then failed for host GPU capacity; no further compute started. Credential is outside the repository, reserved for this experiment and must be revoked afterward. The user subsequently approved replacement flexibility within the original total budget/deadline; see the final execution outcome below. The stopped-pod control test alone did not verify a live watchdog.

- The approved replacement v3 execution on 2026-09-15 is now finalized as FAILED/INCOMPLETE. Read analysis/report_labeling_llm_v3_execution_v1/RESULTS.md and EXECUTION.md. Exact plan unchanged: control-1 completed 5 reports (48/60 technical passes); candidate-1 produced 3 schema-invalid responses then truncated at 4096 tokens on report 4 (0/48 passes). Nine calls recorded, eight complete, eleven unrun; no repeats/paired winner/semantic accuracy. All 28 remote artifacts hash-verified in state/runs/report-labeling-llm-v3-execution-executed-20260915; original preparation preserved. External provider stop confirmed EXITED and console $0/hr; temporary account key disabled and local token removed. Estimated cumulative cost ~$0.44 including prior setup. Watchdog was armed but its CLI stop did not fire. No automatic restart/retry/budget extension. The separate diagnostics/ operator dashboard supports the recorded partial attempt without weakening the complete-session review gate. Qualified review and all earlier adjudications remain unresolved. Future local fixes must be versioned; any further paid generation needs a new concrete reviewed plan and resource authorization.

- The LOCAL candidate_failure_diagnostic_v1 audit is complete. Read analysis/candidate_failure_diagnostic_v1/RESULTS.md and PROTOCOL.md. All 9 rendered-input/token and output/EOS roundtrips matched the pinned assets, all 10 preflight input counts reconstructed, and both child receipt/cache bindings passed. Candidate short outputs: 36 string states with missing nested evidence/confidence. Truncated output: fenced array, 4096 answer-surface tokens, no thought delimiters/EOS, 95 stripped lines beginning with `{"evidence_text"` (11 distinct), one line repeated 11 times. This corrects any assumption that the v3 truncation was a reasoning-envelope overrun. No weights, generation, gold evaluation, parser repair or provider calls. All181 historical analysis files preserved. The next prospective contract-focused correction is not implemented or approved for paid testing; no causal/semantic superiority claim.

- The Qwen pivot is LOCAL-only under analysis/qwen_report_extraction_v1/. Read README.md, PROTOCOL.md and RESULTS.md. It audits saved Qwen v1 outputs (58/60 twice) and prepares exact existing v1/v2 prompt packages; neither new arm has run. Shared v2 parser sensitivity: 56 stay valid, two whitespace failures become diagnostic-valid, two repeated exact spans become ambiguous; original outputs/metrics unchanged. Preserve all189 prior analysis files and 40/18 split. Twelve inherited semantic-review items remain unresolved. The proposed ABBA 20-generation Qwen-only smoke has no execution adapter or current resource approval. No automatic MedGemma schema-contract continuation. No new weights, generation, validation/bulk extraction or MRI training during local preparation.

- Qwen local runner/review now exists separately in analysis/qwen_runtime_v1/. Read PROTOCOL.md and RESULTS.md. It preserves203 historical analysis files, exact Qwen proposal, five inputs and40/18 split. Both CLI and direct backend reject GPU loading; no policy edit unlocks it. Real input preparation is complete; only fabricated CPU sessions ran. Durable attempt/raw/primary/secondary ledgers and partial diagnostic accounting are available. Blinded review excludes historical reference-containing artifacts; finalization never loads organizer labels and leaves comparison deferred. Serve only blinded/. A later execution proposal must implement current resource authorization and provider shutdown controls; old MedGemma budget/key cannot be reused implicitly. No training/bulk/validation is authorized.

- The separate analysis/qwen_execution_v1/ is a resource proposal only. Read PROTOCOL.md, RESULTS.md and configs/resource_proposal.json. It proposes one 60-minute, $1.50 maximum RTX 6000 Ada Qwen smoke and requires a fresh approval bound to exact plan/proposal hashes, a live pod-local watchdog, exact cache/GPU checks, independent decoding of saved output tokens, and external provider stop verification. `execution_enabled` remains false; this package does not unlock qwen_runtime_v1 or start paid compute. Recheck actual RunPod quote, capacity and cache before any new execution plan. Keep the 40/18 split and all historical analysis files unchanged.

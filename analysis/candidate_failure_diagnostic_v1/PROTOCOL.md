# Offline candidate-failure audit v1

Scope: audit the nine durable model calls from the failed v3 attempt, without changing that experiment. No model weights, generation, training, provider actions, organizer-label evaluation, parser repairs or accepted-label conversion. This diagnostic is separate from all 181 protected historical analysis files, anchored at `77c92982b102fc530c07f20fffb5c91300fcf114` in [protected_history.json](protected_history.json).

Bind the source to the independently transferred 28-file inventory SHA-256 `19f31b50f361f911cc3f139f8a96dddadd0cfd377a3450644f1368bccbf83ea7` and unchanged runtime-plan SHA-256 `6bcbee08e0bdb30f148f6a7a84c75afdcd327dc45c94b03b82724f0b625f1ee1`. Verify the full file set and hashes using the frozen diagnostic reader before interpreting responses. Organizer reference files are checksum-checked as opaque bytes for preservation only; their label values are not interpreted or supplied to the analysis.

1. Audit parent/session and both child result bindings, dispatch order, grant/watchdog snapshot hashes, timeline, exact artifact hashes, model/config/runtime metadata, all 13 entries in the original cache-audit receipt, and four candidate raw/parsed pairs. Verify stop after the fourth candidate response and absence of later worker dispatches. Reuse the original completed-run verifier for control 1. Do not weaken the complete-session gate.
2. Check the cached processor/tokenizer assets against the previously verified pinned asset hashes. Use `google/medgemma-1.5-4b-it` revision `91850547d9f0b2fdd21aa7c5f4f3d1a8a52c243b`, the same ten package versions (CPU PyTorch 2.8.0 locally) and the frozen `HFEncoder` offline. Never construct a model/backend. Recompute all ten preflight input counts, and compare all nine recorded rendered prompts and input-ID sequences exactly.
3. Decode every saved output-ID sequence exactly, with special tokens retained and cleanup disabled. Reproduce the original removal of one terminal EOS token for the raw-text field; separately verify the full decoded field. Recompute completion status from EOS, elapsed time and output length. Do not truncate, add tokens or repair text.
4. Classify the first three candidate JSON objects using the frozen normalization result: exact condition keys, value types and whether string values are state names. Never treat structurally invalid values as accepted labels.
5. Locate thought delimiters and terminal EOS in the truncated response. Count exact repeated 16-token windows, including overlapping windows, and exact repeated object-shaped lines. Inspect the opening surface for an array versus required keyed object. These counts describe the output; they do not prove a model-internal cause. No JSON fragments or lines are rescued as predictions.

Private output contains per-call indices, token-layout diagnostics and hashes. Public output contains aggregate structural findings, software/config evidence and limitations. No reports, raw responses, study IDs, keys or weights are published. Output directories must be new. Frozen predictions and the official failed outcome remain unchanged.

A saved-receipt audit is not a fresh GPU replay, provider attestation or independent proof of past process liveness. Model weights are not rehashed locally; only their original complete audit receipt and transfer binding are verified. Exact token reconstruction rules out detected differences in serialization/decoding for these calls, not every possible inference fault or semantic error.

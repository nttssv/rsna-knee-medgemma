# Execution revision review

This proposal follows ChatGPT's confirmation that the local v3 runtime/blinded-review milestone was complete at `0353ecb880fd9bc20a78c602508ff7152a7d5ad5`. It adds resource authorization and lifecycle controls without redesigning the frozen experiment.

The actual paid request is separate from code preparation or advisory review: one stopped RTX 6000 Ada pod, unchanged five reports, maximum 20 generations, proposed $2 budget and 90-minute provider deadline. The source is conditional on private explicit approval, live watchdog verification, current rates and exact plan hashes. At source review time no real approval had been recorded; the user subsequently approved the run and replacement deployment described in [SETUP_ATTEMPT.md](SETUP_ATTEMPT.md).

The watchdog is a best-effort provider stop backstop; external operator monitoring and provider-state verification remain required. Only its local command construction, waiting and bounded failure behavior are exercised by CPU tests. At source-review time no remote stop, allocation or GPU generation was claimed. The later measured execution is recorded below.

ChatGPT reviewed source commit `43d2a6396b2e7e691473f6b52c70716809d190ce` and found no concrete source blocker to presenting this exact paid proposal to the user. It confirmed the plan/proposal/resource gate, preserved bounded runtime and documented operational limits. GitHub CI run `34942347688` passed on that source; all 437 local tests passed. The remaining actions are live quote/allocation checks, explicit user resource approval, remote watchdog verification and independently confirmed provider shutdown. Advisory review is not that approval.

The approved setup attempt did not reach inference because the actual provider key failed the required read preflight. The pod was stopped and the operational evidence was sent to the same ChatGPT conversation for review; ChatGPT confirmed the operational blocker and recommended testing a dedicated temporary control credential against the already-stopped pod before any further paid allocation. It cautioned that GraphQL Read/Write is account-wide and that the observed read denial does not establish that all pod-scoped keys lack self-stop permission. Software/source review did not establish provider permission.

## Live continuation and measured outcome

The user separately approved the temporary account control credential and replacement-host flexibility within the same total $2 and original deadline. ChatGPT reviewed the operational continuation while the unchanged plan was running and found no concrete protocol/provenance blocker. It requested cumulative cost accounting, private allocation/receipt evidence, a distinction between stopped-pod GraphQL tests and the live watchdog CLI stop path, verified transfer/shutdown, and immediate credential revocation. Those requirements are recorded in [EXECUTION.md](EXECUTION.md) and [RESULTS.md](RESULTS.md).

The run subsequently stopped on candidate truncation. This is a preserved failed experiment, not a passed clinical gate. All 28 files were transferred/hash-verified; external GraphQL stop and console $0/hour verified; temporary key disabled and local token removed. No automatic retry or further generation followed. The CPU-only diagnostic viewer is outside the frozen executed source directories and does not unlock the incomplete paired review.

## Final published-result review

ChatGPT reviewed commit `7e8c75acff204ee1f2d3a6c7831d25e14f3f6c38` in the user's existing review conversation. It found no substantive provenance problem with the frozen result and confirmed CI `34951383688` succeeded. Its minor wording correction was adopted: the candidate failed on four attempted reports; the fifth and second repeat were not run. No counts, outputs or executed sources changed.

It identified a bounded follow-up gap: the partial diagnostic checks the inventory, exact prepared inputs/prompt/context and raw-response reparse, but it does not implement the complete equivalent of `verify_run()` for a failed child. The successful control received the original complete-run verifier; the failed candidate has not received that additional offline audit. This is a limitation, not evidence of a detected discrepancy.

The recommended next stage is a separate read-only `candidate_failure_diagnostic_v1`: reconstruct all nine rendered prompts/input token sequences using pinned tokenizer assets; decode output IDs back to exact raw text; classify the three short candidate objects without rescuing them; locate thought/answer markers and repetition in the truncated response; and audit failed-child dispatch/parent/cache/runtime/preflight/artifact bindings. Do not read organizer answers, load model weights, generate responses, change the parser or start paid compute. The first three candidate responses totaled 406 tokens; the fourth used 4,096, so short schema failure and long truncation should be investigated separately. This next stage is **not performed in this publication**. No qualified semantic review or new generation is implied by advisory review.

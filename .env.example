# Export these in your shell or your provider's secret manager.
# The application does not automatically source this file.
RSNA_STATE_DIR=/path/to/persistent/rsna-knee-state
# HF_TOKEN is optional when the Hugging Face CLI is already authenticated.
# HF_TOKEN=<read token with existing MedGemma model access>
# KAGGLE_API_TOKEN=<existing Kaggle token, needed only for data download>

